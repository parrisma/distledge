const Contract = (props) => {

    return (
        <div className="info-container">
            <div className="info">
                <p>INFO:: {props.message}</p>
            </div></div>
    );
};

export default Contract;
