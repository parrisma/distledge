import { addressConfig } from "../constants";
import { getWeb3Accounts } from "@/lib/AccountUtil";
import { useState, useEffect } from "react";

const Contract = (props) => {
  return (
    <div>
      <p>Listed Contract Example</p>
    </div>
  );
};

export default Contract;
