const Contract = (props) => {

    return (
        <div className="warning-container">
            <div className="warning">
                <p>WARNING:: {props.message}</p>
            </div></div>
    );
};

export default Contract;
